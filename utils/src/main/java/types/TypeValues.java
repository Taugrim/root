package types;

public enum TypeValues {
    SELPH("selph"),BREAK("break"),LOGICBREAK("logic"),DEL("del"),NULL("NULL"),EMPTY("empty");

    private final String type;

    TypeValues(String type) {
        this.type=type;
    }

}
